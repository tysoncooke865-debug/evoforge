/**
 * OPTIMISE_PLAN P2 — web twin of url-polyfill.native.ts: browsers ship a
 * real WHATWG URL, so the web bundle carries no polyfill. Intentionally
 * empty; the native twin does the importing.
 */
export {};
