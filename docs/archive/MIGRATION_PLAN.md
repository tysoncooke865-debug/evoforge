# EvoForge Platform Migration: Streamlit → Expo (React Native + TypeScript) + Supabase

## Context

EvoForge is a fitness-RPG (workout logging → XP → evolving avatar, 64 achievements, leaderboard, OpenAI-vision physique analysis) currently built on Streamlit + Supabase. Streamlit is hitting its ceiling: rerun-the-whole-script rendering, session_state contortions for toasts, a JS-readable auth cookie forced by platform limits, ~6 MB of base64 images per render, and 1,570 lines of CSS fighting Streamlit's DOM. The team wants: (1) the exact same look, format, and functionality on a more robust stack, (2) a path to competitive live-action multiplayer mini-games, and (3) distribution on the iOS/Android app stores. The repo's own `ARCHITECTURE.md §5` already anticipated this migration and aimed to keep `domain/` framework-free for it.

> **Correction (Phase 1, measured):** `domain/` is *not* framework-free today. Of its 15 modules only `domain/xp.py` imports nothing from `pandas`, `data/`, `ui/` or `streamlit`; the other 14 reach for `data.sb_ops` at module scope. They all still *import* cleanly without Supabase secrets, so `tools/gen_fixtures.py` can drive them — but "port all ~14 domain modules" below really means *port the pure functions now, and defer the DB-coupled readers (`load_log`, `workout_summary`, `check_achievements`, `calculate_avatar_stats`) to Phase 3*, where TanStack Query tests guard them instead of goldens.

**Repo state:** working tree clean, `main` pushed to GitHub — the current version is already saved. This plan adds the rewrite branch.

## Selected stack (one pick per layer)

| Layer | Choice | Why |
|---|---|---|
| App framework | **Expo (React Native + TypeScript, Expo Router)** | One codebase → iOS + Android (app stores via EAS) + web (react-native-web). Real URLs on web, deep links on native. React/TS skills transfer from web experience; Flutter (Dart, canvas-rendered web) and Next.js-first (defers stores, guarantees a 2nd migration) rejected. |
| Backend | **Supabase — unchanged** | The 13-table Postgres schema, RLS per-user isolation, `xp_total()`/`leaderboard_top()` RPCs, and anti-cheat `xp_events` trigger all survive untouched. Supabase Realtime covers multiplayer phase 1 (presence, challenges). Biggest de-risker of the whole migration. |
| AI calls | **Supabase Edge Functions (Deno/TS)** | Moves `OPENAI_API_KEY` server-side (it can't ship in a client app). Same vendor, no new ops surface. Photos stay in-memory, never stored — same as today. |
| Styling / animation | **NativeWind v4 + react-native-reanimated/Moti** | The 56 `:root` design tokens port near-mechanically (467 was a miscount — there are 500 `var(--x)` *usages*); the 12 keyframes become shared Reanimated animations. RN New Architecture supports `boxShadow`/`filter` for the neon glow. |
| Charts | **Hand-rolled `<LineChart>` (react-native-svg + d3-shape, ~150 lines)** | App only uses simple time-series lines; full control of neon styling; avoids Victory Native's ~7 MB CanvasKit wasm on web. |
| State | **TanStack Query (server) + Zustand (UI)** | Query replaces all 4 hand-rolled cache layers (per-user keys, invalidate-on-write, `clear()` on sign-out). Zustand holds toast queue / XP burst / perf-mode flags. |
| Auth storage | **supabase-js: SecureStore adapter on native, localStorage on web** | `autoRefreshToken` deletes the entire hand-rolled cookie/rotation saga in `auth/persistence.py`. React's default escaping kills the XSS vector that made the JS-readable cookie dangerous. Security upgrade by deletion. |
| Web hosting | **Cloudflare Pages** (static `expo export -p web`) | Free, auto-deploys from GitHub, per-branch preview URLs — the rewrite gets a live URL on every push while Streamlit stays live on `main`. |
| Store shipping | **EAS Build + EAS Submit** | No local Xcode/Android toolchain; two-person team ships from CLI/CI. |

## Repo / branch strategy

- New long-lived branch **`expo-rewrite`**, pushed to origin so both teammates can work on it. Merge to `main` only at cutover; Streamlit keeps deploying from `main` throughout.
- New directories alongside the untouched Python app (do **not** name the Expo dir `app/` — Expo Router reserves that for routes, and `app.py` exists):

```
evoforge/
├── app.py, views/, domain/, data/, ...   # Streamlit — untouched, still deployed
├── migrations/          # shared — remains THE schema source of truth
├── avatar_assets/       # shared — same 10 PNGs
├── contracts/fixtures/  # NEW: golden JSON generated by Python, asserted by TS tests
├── supabase/functions/  # NEW: ai-physique / ai-bodyfat / ai-coach / ai-plan
└── client/              # NEW: the Expo app (own package.json, no monorepo tooling)
    ├── app/             # Expo Router routes (18 screens)
    ├── src/domain/      # TS port of the ~14 pure Python domain modules
    ├── src/data/        # supabase-js + TanStack Query hooks
    ├── src/ui/          # Card, XPBar, AvatarStage, Toast, LineChart, RarityBadge…
    ├── src/theme/       # tokens ported from assets/styles.css :root + animations.ts
    └── assets/avatars/  # 10 PNGs + build-time-generated locked silhouettes
```

## What stays exactly as-is

DB schema (13 tables, `DEFAULT auth.uid()` tenancy), all RLS policies, both RPCs, the append-only XP ledger + guard trigger, the hand-run numbered-SQL migration workflow, the 10 avatar PNGs, every design-token *value* (colors, glows, radii, durations), and every game rule as data: XP curve `500 + (L-1)*25`, 10 XP/set, 2 XP/cardio-min, rarity thresholds 25/50/75/100, branch rules, evolution names, `ROUTINE`, `EXERCISE_LIBRARY`, all 64 achievements, `resolve_xp` floor-at-derived + drift semantics.

Two exceptions surfaced while building the Phase 1 goldens, both recorded in `contracts/fixtures/`:

- **`evolution_name` was fixed, not preserved.** It promoted an aesthetic athlete to "True Adam" at `level >= 90`, contradicting the `true_adam` achievement ("Reached level 100."), `RANK_TIERS`, `avatar_stage_rows` and `avatar_rarity`, all of which say 100. Corrected to `>= 100`.
- **Two rarity palettes ship simultaneously and are pinned as-is.** On one avatar card, `ui/avatar_cards.py` sets a `rarity-{slug}` class (aura, driven by the CSS `:root` tokens) *and* injects `rarity_badge_html()` (badge, driven by Python's `avatar_rarity()`). Only COMMON agrees; Python's EPIC `#38bdf8` is literally the CSS `--rare`. The port reproduces the mismatch. Unifying them is a deliberate visual change and does not belong inside a framework migration.

## View mapping (18 → Expo Router)

`auth` → `(auth)/sign-in|sign-up` · `onboarding` → `/onboarding` (gate = profile row exists, unchanged) · `home` → `(main)/index` · `today, routine, cardio, bodyweight, bodyfat, measurements, physique, avatar, progress, goals, achievements, leaderboard, profile` → same-named `(main)/*` routes · `data_manager` → `(main)/settings/data` · `delete_data` → `(main)/settings/delete`. Root `(main)/_layout` reproduces today's shell: sidebar nav ≥1024 px web, bottom tabs + sheet on mobile/native.

**Hard-to-port replacements:** session_state toast pumps → Zustand store + `<ToastHost>` with Moti (no reruns exist anymore); `st.rerun()`/cache invalidation → TanStack `invalidateQueries`; Pillow silhouette pipeline → one build-time script `tools/gen_locked_avatars.py` (reuses existing Pillow code once) emitting static `*_locked.png`; `st.file_uploader` → `expo-image-picker` + resize to ~1024 px; ZIP backup → client-side `fflate` (+ share sheet on native); Streamlit-DOM CSS selectors → deleted, components own styles; `ui/escape.py` → obsolete (React escapes by default; ESLint bans `dangerouslySetInnerHTML`); perf-mode `:has()` hack → Zustand flag + honor reduced-motion.

## Auth flow

supabase-js client with `autoRefreshToken + persistSession`; storage = SecureStore-AES adapter (native, Supabase's documented pattern) / localStorage (web). Email confirm links: `https://<web-host>/auth/callback` + `evoforge://auth/callback` deep link, both registered in Supabase Auth settings. Root layout listens to `onAuthStateChange`: no session → `(auth)`; session without profile row → `/onboarding`; else `(main)`. Sign-out = `supabase.auth.signOut()` + `queryClient.clear()` + Zustand reset. Wire `AppState` → `startAutoRefresh()/stopAutoRefresh()` on native.

## AI via Edge Functions

Four JWT-verified functions (`ai-physique`, `ai-bodyfat`, `ai-coach`, `ai-plan`) mirroring `services/*`. Image arrives base64, goes to OpenAI Responses API, discarded — never stored. Each function **writes the result row itself with the caller's JWT** (RLS applies; client can't forge scores) and returns it. Cost controls: per-user hourly rate limit + `sha256(image)` result cache in a new `ai_scan_cache` table → `migrations/007`.

## Testing (replaces the AppTest harness)

1. **Cross-language parity fixtures (keystone):** `tools/gen_fixtures.py` runs the *existing Python* `domain/` and writes golden JSON to `contracts/fixtures/` — full XP curve L1–100, `resolve_xp` branches, rarity/branch boundaries (24/25, 49/50, 74/75, 99/100), navy body-fat samples, achievement sweeps. Vitest asserts the TS port reproduces every fixture exactly; CI runs both sides (detects Python drift AND port drift). No screen work before this is green.
2. Vitest + RN Testing Library for toast store, XPBar math, delete-by-id.
3. Playwright E2E on the exported web build vs a seeded staging user (sign-in → log set → XP toast → refresh keeps session → sign-out clears).
4. `tools/verify_rls.py` is frontend-agnostic — **kept as-is**.
5. Maestro native smoke flows at Phase 7. Existing Python CI keeps running (path-filtered) until cutover.

## Phases

| # | Deliverable / milestone | Effort |
|---|---|---|
| 0 | **Branch + foundations:** create & push `expo-rewrite`; scaffold `client/` (Expo Router, NativeWind, TanStack Query, Zustand); port design tokens; supabase-js auth incl. SecureStore + deep links; Cloudflare Pages preview; CI skeleton. *Milestone: sign in on web preview and Expo Go.* | S–M |
| 1 | **Domain parity:** port all ~14 domain modules to `src/domain/`; fixture generator + goldens committed; parity suite green in CI. | M |
| 2 | **Read-only slice:** auth screens, onboarding wizard, nav shell, **Home + Avatar** on real data (avatar card, XP bar, rarity, locked silhouettes). *Milestone: your account looks like EvoForge on phone + browser.* | M–L |
| 3 | **Logging loop:** Today (update-in-place sets), Routine, Cardio, Bodyweight, Measurements; mutations + invalidation; toast host + XP burst; achievements sweep. *Milestone: team dogfoods daily, stops using Streamlit for logging.* | L |
| 4 | **AI:** 4 Edge Functions deployed; Physique + Bodyfat screens with image picker; coach + plan gen; rate limit + hash cache (migration 007). | M |
| 5 | **Remaining views:** Progress, Goals, Achievements, Leaderboard (drift refusal), Profile, Data export, Delete. | M |
| 6 | **Parity hardening:** `PARITY.md` checklist per view × platform (screenshots at 1280/390 px, every behavior enumerated, identical domain numbers, both teammates sign off); animations tuned; perf mode; reduced-motion. | M |
| 7 | **Ship:** EAS builds, device-verified deep-link auth, icons/splash, store listings + health-data privacy labels (physique photos!), TestFlight/Play internal, submit. Web cutover: merge to `main`, domain → Cloudflare Pages. | M + review latency |
| 8 | **Multiplayer groundwork (post-migration):** apply/backfill ledger migrations 002/006 (anti-cheat prerequisite); friend/challenge schema; Supabase Realtime for presence + async challenges. Dedicated authoritative game server (e.g. Colyseus on Fly.io) only when a mini-game needs sub-second server-arbitrated state — it slots in as a second WebSocket endpoint, no rework of phases 0–7. | design-only |

## Top risks

1. **Pixel-parity × 3 platforms is a scope trap** → web at 1280/390 px is the parity target; native gets *documented* idiomatic deviations (tab bar vs sidebar). The checklist makes "faithful" finite.
2. **Python↔TS domain drift** (silent wrong XP/levels) → golden fixtures in Phase 1, both sides in CI, before any screens.
3. **Neon glow perf on old Androids** → pre-bake glow into generated PNGs where possible, Reanimated (UI thread) for motion, perf mode ships as a real feature.
4. **Long dark period demotivates a 2-person team** → Streamlit stays live on `main`; branch gets a preview URL every push; Phase 3 makes the new app the daily driver early.
5. **Native auth edge cases** (deep links, background refresh, SecureStore 2 KB limit) → solved in Phase 0 using Supabase's documented RN patterns, before anything depends on them.

## Notes for the AI coding agent implementing this

1. **Never scaffold from memory.** Expo, NativeWind, supabase-js, and Reanimated move faster than any model's training data. Phase 0 starts by running `npx create-expo-app` and reading the *current* official docs for Expo Router, NativeWind v4 setup, and the Supabase RN auth guide; pin every version in `package.json` and record the pinned set in `client/CLAUDE.md`. If an API surface differs from expectation, trust the installed version, not recall.
2. **Every phase ends with a runnable check.** Don't declare a phase done on visual inspection — Phase 0: sign-in succeeds on the deployed preview; Phase 1: `npm test` parity suite green against committed fixtures; Phase 3: log a set end-to-end and watch the XP number change; etc. If a check can't be run, that's a blocker to raise, not a step to skip.
3. **Create `client/CLAUDE.md` in Phase 0** carrying over this repo's doctrine translated to the new stack: no `dangerouslySetInnerHTML` (enforced by ESLint), XP curve numbers are a contract (change only with fixtures), sign-out must clear TanStack Query + Zustand, per-user data isolation relies on RLS — never on client filtering, photos are never persisted anywhere.
4. **Port, don't reinterpret.** When porting `domain/` modules, translate line-by-line semantics first and refactor only after the parity fixtures pass. The fixtures are the arbiter of correctness, not readability preferences.

## Immediate actions on approval

1. Write this plan into the repo as `MIGRATION_PLAN.md`, commit to `main`, push (this "saves the current version" marker — tree is otherwise already clean & pushed).
2. `git checkout -b expo-rewrite && git push -u origin expo-rewrite` — the shared dev branch for the new framework.
3. Begin Phase 0 scaffold on `expo-rewrite`.

## Verification

- After step 1–2: `git log origin/main` shows the plan commit; `git branch -r` shows `origin/expo-rewrite`; teammate can `git fetch && git checkout expo-rewrite`.
- Phase 0 done when: web preview URL loads sign-in and authenticates against the existing Supabase project; same on Expo Go; CI green.
- Ongoing: parity fixture suite + Playwright E2E + `verify_rls.py` as described above; final acceptance = completed `PARITY.md` checklist.
